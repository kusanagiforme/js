总的来说，学这个还是感觉自己和别人差距很大，也不够细心。
总结一下：
1.进行样式的操作时，不能通过单独的把样式赋值给一个变量。（这个属于自己脑子蠢了）

2.计时函数处理要单独赋给一个变量，方便结束方法处理计时方法。

3.每个属于dom的事件都有单独的event对象，指向的是样式对象。有的是指向window（比如鼠标事件和键盘事件）。

4.css sprite是一种可以将图片处理的技术，可以局限显示图片的单一区域，通过js或者css来实现视图在图片上的移动来达到一种动画的效果，js可以通过计时程序来做到。
