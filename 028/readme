
今天赶时间，大概学习了一下别人的写法
1。（函数式写法）{
针对不同的功能，封装成函数可以提高效率
}
2。针对键盘的输入，可以用条件判断语句来逐行分析
键盘的对象是：e.evnet或者e。

3。可以使用js来为添加的元素动态添加上css标签。在css中实现准备好相关的css代码，就可以完成动态选择代码块后与css匹配的功能

4。这次代码中使用的分割用户输出的字符串也是动态的，根据indexOf方法判断需要分割开的位置，再用判断语句和slice方法分割开来。

5。这次使用了ES6的箭头函数
postfixList.forEach((i=>{
			html.push('<li>'+value+'@'+i+'</li>')
		})
		sug.innerHTML = html.join('');
	)
  这段代码创建了一个匿名函数，传入一个i作为参数。而foreach本身是一个数组的迭代方法，可以将传入的参数每个作用与函数，这样箭头函数就可以返回被函数作用后的数组
